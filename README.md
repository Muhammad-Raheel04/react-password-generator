# Password Generator

A simple React password generator built with hooks.
Users can customize password length and include numbers or special characters.
Includes a copy-to-clipboard feature using useRef.
Built using React and Tailwind CSS.
